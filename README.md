# Alignment
Alignment of consecutive slides


1.Load the consecutive images into BigWarp

2. Select corresponding points and save landmark output
   
3. Inputs of the script: consecutive images; bacode and postition csv file of the images; landmark file;
  
4. Run the alignment script
